<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style type="text/css">
      table {
        border-collapse: collapse;
          width: 100%;
        }
        th, td {
          padding: 0.25rem;
          text-align: left;
          border: 1px solid #ccc;
        }
        tbody tr:nth-child(odd) {
          background: #eee;
        }
    </style>
  </head>
  <body>
    <h1>Welcome to ZIGExN VeNtura, <%= @order.billing_information.name if @order.billing_information %></h1>
    <table>
      <thead>
        <tr>
          <th>No</th>
          <th>Product Name</th>
          <th>Price</th>
          <th>Qty</th>
          <th>Amount</th>
        </tr>
      </thead>
      <tbody>
        <% @order.cart.line_items.each_with_index do |item, index|%>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= item.product.try(:name)%></td>
          <td><%= item.product.try(:price)%></td>
          <td><%= item.qty%></td>
          <td><%= item.amount%></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <p>
      To login to the site, just follow this link: <%= @url %>.
    </p>
    <p>Thanks for joining and have a great day!</p>
  </body>
</html>