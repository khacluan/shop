<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Shop" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "frontend", :media => "all" %>
    <%= javascript_include_tag "frontend" %>
  </head>
  <body>
      <input type="hidden" name="action-name" value="<%= params[:action] %>" id="action_name_store"/>
      <%= render :partial => "shared/header" %>
      <% if !["profile", "checkout", "update_billing_information", "search", "home"].index(params[:action])%>
          <%= render :partial => "shared/breadcrumb.html" %>
      <% end %>

      <% if ['home'].index(params[:action]) %>
        <%= yield %>
      <% else %>
        <div class="container main-box" role="main">
          <%= yield %>
        </div>
      <% end %>
      
      <%= render :partial => "shared/footer" %>
  </body>
</html>
